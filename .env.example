# Copy to .env and fill in your Gemini / Google API Key
GEMINI_API_KEY=your_google_api_key_here
# Optional: override default model (defaults to gemini-2.5-flash if unset)
GEMINI_MODEL=gemini-2.5-flash
# Optional: maximum steps per agent.run (defaults to 8)
AGENT_MAX_STEPS=8
